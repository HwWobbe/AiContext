# ᢳＨ — WaysH

**One line. Act. Done.**

---

## Index

| ᢳＨ | What |
|---|---|
| `ᢳＨ:CommitPush` | Save changes to GitHub |
| `ᢳＨ:OpenFile` | Open a file for editing |
| `ᢳＨ:PreviewMd` | Render a markdown file |
| `ᢳＨ:CopyToRepo` | Place a file in the right repo folder |
| `ᢳＨ:CheckStatus` | Confirm what Git sees |
| `ᢳＨ:JumpToNote` | Navigate to a stamped note |
| `ᢳＨ:NewTiddler` | Create and stamp a TW tiddler |

<div style="page-break-after: always;"></div>

---

## Rule

One line per environment. If you need to read more — it's too long.

---

## ᢳＨ:CommitPush

| Env | Action |
|---|---|
| 𝒢 GitHub Desktop | Summary box → **Commit to main** → **Push origin** |
| ℱ VS Code | `Ctrl+Shift+G` → type message → ✓ Commit → Sync |
| 𝒮 PowerShell | `git add .` → `git commit -m "msg"` → `git push` |

---

## ᢳＨ:OpenFile

| Env | Action |
|---|---|
| ℱ VS Code | `code <filename>` in command prompt |
| 𝒮 PowerShell | `start <filename>` |
| ℱ Explorer | Navigate → double-click |

---

## ᢳＨ:PreviewMd

| Env | Action |
|---|---|
| ℱ VS Code | Open file → `Ctrl+Shift+V` |
| 𝒢 GitHub | Navigate to file → renders automatically |
| 🌐 Browser | Press `.` on any GitHub repo page |

---

## ᢳＨ:CopyToRepo

| Env | Action |
|---|---|
| 𝒮 PowerShell | `copy <src> C:\Users\hwobb\GitHub\AiContext\ContextLatticeStamp\Docs\` |
| ℱ Explorer | Download → navigate to Docs folder → paste → replace |
| ℱ VS Code | `code C:\Users\hwobb\GitHub\AiContext\ContextLatticeStamp\Docs\<file>` → Ctrl+A → paste → Ctrl+S |

---

## ᢳＨ:CheckStatus

| Env | Action |
|---|---|
| 𝒢 GitHub Desktop | Left panel → Changes tab |
| 𝒮 PowerShell | `git status` |
| 𝒢 GitHub | `github.com/HwWobbe/AiContext` → verify content |

---

## ᢳＨ:JumpToNote

| Env | Action |
|---|---|
| 🖥 Desktop | `Win+J` → lane → key → Enter |
| ⌨ Text field | `/jt <key>` → paste in terminal |
| 𝒞 AI chat | `/f <key>` → click URL |
| 𝒮 PowerShell | `python -c "from rnhw_json_manager import find_notes..."` |

---

## ᢳＨ:NewTiddler

| Env | Action |
|---|---|
| 𝒯 TwSf | Click **+** → title → content → ✓ |
| 𝒯 TwNode | Click **+** → title → content → ✓ |
| 𝒯 Stamp it | Register via `/reg` or PowerShell after saving |

---

*Part of the [AiContext](https://github.com/HwWobbe/AiContext) collection.*

---

╏0:trailer pref saved╏3:⏳S:~15% W:~10%╏8:📿¹╏F:∇ᴷ⌱⟀╏
